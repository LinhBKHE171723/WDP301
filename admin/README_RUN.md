# Admin UI (Blue Clean) for CRA
Install deps and run:

```
npm install
npm start
```

- Tailwind is configured.
- Routes: /, /analytics, /accounts, /feedback, /settings
- UI components in `src/components/ui`.
